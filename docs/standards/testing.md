# Testing Standard

Foundation packages require unit tests for successful behavior, boundaries, invalid input, equality, and operators. Architecture tests enforce dependency rules. CI collects Cobertura and LCOV output. Production modules will add integration, contract, and end-to-end suites.
