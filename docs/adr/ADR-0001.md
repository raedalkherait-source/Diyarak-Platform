# ADR-0001: Use .NET 10 LTS

**Status:** Accepted

## Decision

The foundation targets .NET 10 because it is an active LTS release. SDK selection is pinned by global.json while patch roll-forward remains enabled.
