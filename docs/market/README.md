# Diyarak Market 1.0

This area records concrete Diyarak Market product and domain requirements before implementation.

## Confirmed scope

- Property module.
- Company module.
- Listing module.
- Public and administrative APIs.
- Search.

## Requirements

- `property-listing-requirements.md` records the currently confirmed Property and Listing concepts derived from the supplied Market reference flow.
- `listing-subject-reference-requirements.md` records the confirmed subject-reference and publication-availability requirements, the decisions implemented by ADR-0010 through ADR-0039, and the remaining unresolved Listing operations beyond creator direct/collection loading and Draft editing, external-identity provisioning transport, ownership transfer, and post-publication availability behavior.

## Architectural constraint

A Property is a persistent domain asset; a Listing is a market publication with an independent lifecycle. See `../adr/ADR-0006.md`. Market-specific Listing behavior belongs to `Diyarak.Market.Listing` while Platform Listing remains sector-agnostic; see `../adr/ADR-0008.md`.

ADR-0010 defines the sector-agnostic `ListingSubjectReference` contract and explicitly approves the `Diyarak.Market.Listing` dependency on `Diyarak.Platform.Listing`. ADR-0011 keeps that subject reference immutable for the lifetime of a Listing. ADR-0012 assigns supported subject-type validation to the consuming business module. ADR-0013 defines the initial Market Listing lifecycle as `Draft` followed by an explicit transition to `Published`. ADR-0014 defines the core publication-readiness requirements. ADR-0015 restricts core Listing edits to the `Draft` state.

ADR-0016 requires application-level subject-availability validation before publication. ADR-0017 treats Property existence as availability until a Property lifecycle exists. ADR-0018 introduces the Application layer for cross-module orchestration. ADR-0019 defines the Application-owned `IPropertyExistenceChecker` port and keeps `Diyarak.Market.Application` independent of `Diyarak.Market.Property`.

ADR-0020 keeps EF Core persistence records and mappings inside Integrations, requires explicit translation through valid domain APIs, and permits Integrations to implement Application-owned ports without introducing infrastructure concerns into business Modules.

ADR-0021 places Listing loading and saving inside publication orchestration through the Application-owned `IMarketListingRepository` port. ADR-0027 adds the explicit Application-owned transaction boundary: creation and publication persistence-sensitive work runs in one PostgreSQL `ReadCommitted` transaction. ADR-0028 first adds optimistic publication concurrency. ADR-0029 generalizes that protection to an explicit numeric Listing version and defines authenticated owner-only partial Draft editing through `PATCH /api/market/listings/{listingId}`. Property row locking and stronger isolation remain undefined.

ADR-0022 defines the future Listing publication HTTP contract and its stable validation, not-found, and conflict error codes.

ADR-0023 assigns every Listing an immutable non-empty `PublisherUserId` and permits publication only for that authenticated creator. A non-owner receives the same not-found result as a missing Listing, and ownership is checked before Property existence, domain mutation, or persistence.

ADR-0024 adds `publisher_user_id` as a required PostgreSQL `uuid` without a default. The ownership migration stops before changing `market.listings` when legacy rows require an authoritative ownership mapping.

ADR-0025 makes `Diyarak.Api` an OAuth 2.0 resource server for explicitly configured OIDC access tokens, resolves exact external `(iss, sub)` identities to internal users, and exposes authenticated Market Listing routes only for mapped users.

ADR-0026 defines authenticated creation of a `Draft` Market Listing for an existing Property. The request supplies only `propertyId`; the mapped internal actor becomes the immutable `PublisherUserId`, and the new aggregate is inserted through the Application-owned repository port.

ADR-0030 defines authenticated Property creation through `POST /api/market/properties`. ADR-0031 adds authenticated direct Property loading through `GET /api/market/properties/{propertyId}`. ADR-0034 establishes immutable internal management ownership for newly created Properties and converts direct loading to owner-only concealed access; legacy rows remain nullable/unowned until an authoritative mapping exists. ADR-0035 adds mapped-user-only `GET /api/market/properties` for the current owner's management collection, filtered by persisted `OwnerUserId` and intentionally without search, pagination, or ordering semantics. ADR-0037 requires the mapped Property owner for `POST /api/market/listings`; missing, non-owned, and legacy-unowned Properties remain concealed behind the existing creation conflict. ADR-0032 resolves the Listing creation `Location` with creator-only `GET /api/market/listings/{listingId}`, returning current management state while preserving concealed `404` responses for non-owners. ADR-0033 adds mapped-user-only `GET /api/market/listings` for the authenticated creator's management collection, filtered by persisted ownership and intentionally without search, pagination, or ordering semantics. ADR-0038 adds anonymous `GET /api/market/public/listings/{listingId}` for `Published` Listings only, with a deliberately reduced public projection that omits ownership, version/status, and subject identifiers. ADR-0039 adds anonymous `GET /api/market/public/listings` with bounded `page`/`pageSize` pagination, deterministic `ListingId` ordering, `hasMore`, and the same reduced public projection; it does not define search/filter/ranking semantics.

## Current implementation status

- `Diyarak.Market.Property` has an initial domain baseline with aggregate identity, supported top-level property categories, a required property address, optional living, usable, sales, and total areas, optional room counts, optional furnishing quality, optional property features, optional building years, optional parking-space count, and optional commercial-property subtype.
- `PropertyAddress` captures street, house number, postal code, city, and an optional geographic location using the shared `GeoCoordinate` primitive.
- Living, usable, sales, and total areas use the shared `Area` primitive and remain optional.
- Total rooms, bedroom count, and bathroom count are optional and reject negative values.
- Furnishing quality is optional and supports the confirmed values `Simple`, `Normal`, `Upscale`, and `Luxury`.
- Property features are optional, de-duplicated, and limited to the currently confirmed feature set.
- Construction year and last modernization year are optional; positive values are accepted and `null` represents an unknown or unspecified year.
- Parking-space count is optional and rejects negative values.
- Commercial-property subtype is optional, limited to the confirmed subtype set, and can only be assigned when the top-level category is `CommercialProperty`.
- `Diyarak.Market.Property.Tests` verifies aggregate invariants, address validation and equality, geographic-location assignment, optional living, usable, sales, and total area assignment, room-count validation, furnishing-quality validation, property-feature validation, building-year validation, parking-space-count validation, and commercial-subtype rules.
- Authenticated owner-only direct Property loading by identifier is implemented through ADR-0031 as superseded by ADR-0034. New Properties have immutable internal management `OwnerUserId`; legacy rows may remain unowned. ADR-0035 adds the current mapped owner's management collection and excludes both other users' and legacy-unowned rows. Property search, public presentation, update, removal/archive, ownership transfer/delegation, and administrative access remain undefined. Additional Property characteristics documented in `property-listing-requirements.md` are not yet implemented.
- `Diyarak.Market.Company` remains a foundation scaffold pending concrete company requirements.
- `Diyarak.Market.Listing` has an initial aggregate baseline. `Listing` uses a `Guid` identity, requires an immutable non-empty `PublisherUserId`, requires a sector-agnostic `ListingSubjectReference`, keeps both values immutable for its lifetime, and currently accepts only the supported `market.property` subject type.
- `ListingContext` combines the confirmed publishing roles `Owner`, `Tenant`, and `ProfessionalOrAgent` with the confirmed transaction intentions `Rent`, `Sell`, and `RentForLimitedPeriod`.
- `ListingPrice` represents either a known non-negative `Money` amount or price on request.
- `ListingHeadline` represents a non-empty listing headline without imposing an undocumented maximum length.
- `ListingAvailableFromDate` represents the confirmed available-from calendar date using `DateOnly` without imposing undocumented past/future validation.
- `MarketListingSubjectTypes.Property` defines the stable sector-qualified `market.property` subject type consumed through the Platform Listing subject-reference contract without creating a dependency on `Diyarak.Market.Property`.
- `Diyarak.Market.Listing.Tests` verifies Listing identity, required immutable publisher ownership, rejection of an empty publisher identifier, required subject-reference assignment, rejection of unsupported Market subject types, initial `Draft` status, publication-readiness enforcement, the `Draft` to `Published` transition, rejection of repeated publication, rejection of core publication-data edits after publication, the confirmed publishing-role and transaction-intent value sets, `ListingContext` assignment and equality, rejection of unsupported enum values, `ListingPrice` known/on-request behavior and negative-price rejection, `ListingHeadline` assignment, equality, and blank-value rejection, optional `ListingAvailableFromDate` assignment and value equality, and the stable Property subject-type value.
- `Diyarak.Platform.Listing` now provides the sector-agnostic `ListingSubjectReference` value object using a `Guid` subject identifier and a non-empty opaque subject-type string.
- New Market Listings start as `Draft`. `ListingContext`, `ListingHeadline`, and `ListingPrice` are required before `Publish()` can transition the Listing to `Published`; `ListingAvailableFromDate` remains optional. These core publication values can be assigned or changed only while the Listing is `Draft`, and repeated publication is rejected.
- `Diyarak.Market.Application` owns `IMarketPropertyRepository`, `IMarketListingRepository`, `IPropertyExistenceChecker`, `IPropertyListingAuthorizationChecker`, and `IMarketTransactionRunner`. `CreatePropertyUseCase` constructs the Property aggregate from validated current-domain fields and assigns the mapped internal actor as immutable management owner; `GetPropertyUseCase` performs owner-only direct loading with concealed not-found behavior; `ListOwnedPropertiesUseCase` loads the mapped owner's own management collection through an owner-filtered no-tracking query. `CreateListingUseCase` receives a Property identifier and authenticated actor user identifier, verifies owner authorization for that Property, creates an actor-owned `Draft` Listing, and adds it through the repository. `GetListingUseCase` performs creator-only direct loading without a transaction, and `ListOwnedListingsUseCase` loads the mapped creator's own collection through a publisher-filtered no-tracking query. `UpdateListingUseCase` and `PublishListingUseCase` load owner-controlled Listings through the same repository and transaction boundary. Draft edits require the client-supplied Listing version, while publication uses the loaded version; both saves advance the persisted version and reject stale writes with the same stable concurrency conflict.
- `CreateListingErrors` defines creation validation and Property-existence failures through `market.listing.invalid_property_id`, `market.listing.invalid_actor_id`, and `market.listing.property_not_found`. `PublishListingErrors` defines the publication-specific `market.listing.invalid_id`, `market.listing.invalid_actor_id`, `market.listing.not_found`, `market.listing.property_not_found`, `market.listing.concurrent_modification`, and `market.listing.cannot_publish` outcomes.
- `Diyarak.Market.Application.Tests` verifies Listing creation validation, owner-authorized Property enforcement, actor ownership assignment and Draft persistence, owner-only partial Draft editing, explicit version validation, optional available-from clearing, stale-edit conflict, plus publication validation, concealed not-found behavior, missing-Property conflict, publication-state conflict, stale-save concurrency conflict, and successful creator-owned publication and saving.
- `Diyarak.Platform.Persistence.PostgreSql` contains the full-state `MarketPropertyRecord`, including nullable legacy-safe `OwnerUserId`, its explicit EF Core mapping to `market.properties`, conversion to and from `Diyarak.Market.Property.Property`, the `MarketPropertyPersistenceBaseline` migration, and the no-default/no-backfill `MarketPropertyOwnership` migration.
- It also contains the full-state `MarketListingRecord`, including `PublisherUserId` and the numeric `Version`, its explicit EF Core mapping to `market.listings`, conversion to and from `Diyarak.Market.Listing.Listing`, the `MarketListingPersistenceBaseline` migration, the safe no-default `MarketListingPublisherOwnership` migration, and the `MarketListingVersionConcurrency` migration that backfills existing rows to version `1`.
- `PostgreSqlPropertyExistenceChecker` implements `IPropertyExistenceChecker` with a no-tracking key-existence query for publication-time availability.
- `PostgreSqlPropertyListingAuthorizationChecker` implements owner-aware Listing-creation authorization with a no-tracking `Id + OwnerUserId` query.
- `PostgreSqlMarketListingRepository` implements `IMarketListingRepository` and adds, loads by identifier or publisher ownership, and conditionally saves Market Listing state through `PlatformDbContext`; persisted Listing `Version` is configured as the EF Core concurrency token and every successful mutation writes the next version.
- `PostgreSqlMarketTransactionRunner` implements `IMarketTransactionRunner` with a `ReadCommitted` transaction executed through EF Core's configured retry strategy.
- `PostgreSqlMarketPropertyRepository` inserts newly created actor-owned Property aggregates and performs no-tracking direct-by-id loads through the full-state Property record mapping and table.
- `AddPostgreSqlPersistence` registers the Market Application-port adapters as scoped services.
- `Diyarak.Platform.Persistence.PostgreSql.Tests` verifies Property ownership migration safety, Property and Listing table mappings, complete domain-record round trips including publisher ownership and version, found and missing Property checks, Listing repository insertion and loading, versioned saving, stale competing publication and Draft-edit rejection, dependency-injection registration, ownership migration safety, and the version backfill migration.
- `Diyarak.Api` references the Application layer, registers `CreatePropertyUseCase`, `GetPropertyUseCase`, `CreateListingUseCase`, `ListOwnedListingsUseCase`, `GetListingUseCase`, `UpdateListingUseCase`, and `PublishListingUseCase`, and conditionally maps authenticated Market Property/Listing routes when ADR-0025 authentication is enabled. `POST /api/market/properties` creates an actor-owned Property asset from current modeled fields, and `GET /api/market/properties/{propertyId}` loads it only for that owner while concealing missing, non-owned, and legacy-unowned rows; neither route defines public address presentation. `POST /api/market/listings` creates an actor-owned Draft only for a Property owned by the same mapped actor, `GET /api/market/listings` lists the current creator's management resources, `GET /api/market/listings/{listingId}` loads current state for the creator, `PATCH /api/market/listings/{listingId}` performs owner-only versioned partial Draft editing, and `POST /api/market/listings/{listingId}/publish` preserves ADR-0023 concealed not-found behavior for non-owners.
- Property search, update/removal/archive, ownership transfer/delegation, legacy owner assignment, and administrative workflows beyond authenticated actor-owned creation, owner-only direct/collection loading, and existence checks; identity-provider vendor selection and external-identity provisioning transport; other Listing operations beyond the defined creator direct-read, creator collection-read, public published direct/collection-read, and Draft-edit contracts; administrative overrides and ownership transfer; authoritative migration of any legacy Listing ownership; Property row locking or stronger-than-`ReadCommitted` isolation guarantees; behavior when a referenced subject later becomes unavailable; additional publication-readiness requirements; additional Listing lifecycle states and transitions; transaction-specific commercial terms; and published-Listing editing workflows remain deferred pending concrete requirements. Authenticated Market management endpoints are exposed only when ADR-0025 authentication is explicitly enabled and require a mapped internal user. ADR-0038 and ADR-0039 public published reads are anonymous and mapped independently of OIDC management authentication.
- Other public and administrative endpoint requirements are not yet defined.
- Authentication and authorization requirements for administrative APIs are not yet defined.
- Search behavior is not yet defined.

Implementation should not introduce speculative domain fields, lifecycle states, API contracts, or dependencies before concrete requirements are documented.


## Property management replacement (ADR-0036)

Authenticated mapped owners can replace the mutable detail state of an owned Property with `PUT /api/market/properties/{propertyId}`. The request carries the current positive `version` plus the complete Property detail representation. Identity and `OwnerUserId` remain immutable. Missing, non-owned, and legacy-unowned Properties are concealed as `404`; stale versions return `409 market.property.concurrent_modification`. Omitted optional Property fields are cleared because the contract is full replacement, not PATCH. New Properties start at version `1`, and Property management create/get/list responses expose the current version.
